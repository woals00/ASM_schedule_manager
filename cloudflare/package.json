{
  "name": "asm-schedule-alarm-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "db:migrate": "wrangler d1 execute asm-schedule-db --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.18.0"
  }
}
